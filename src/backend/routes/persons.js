const s = require("express"), { worker: e } = require("cluster"), { Persons: r } = require("../database/db"), a = require("../utils/logger/log"), t = s.Router(); t.get("/data/:unique_key", (async (s, t) => { try { let e = {}; const a = s.params.unique_key; e = "all" == a ? await r.findAll() : await r.findOne({ where: { key: a } }), t.status(200).json(e) } catch (s) { a.fatal(`[worker: ${e.id}] unhandleError ./src/backend/routes/persons.js /data/:unique_key : ${s}`), t.status(500).json({ message: "internal server error" }) } })).get("/data/all/class/:class_person", (async (s, t) => { try { const e = "all" == s.params.class_person.toLowerCase() ? void 0 : s.params.class_person || void 0, a = void 0 === e ? void 0 : { where: { class: e } }, o = await r.findAll(a); t.status(200).json(o) } catch (s) { a.fatal(`[worker: ${e.id}] unhandleError ./src/backend/routes/persons.js /data/class/:class_person : ${s}`), t.status(500).json({ message: "internal server error" }) } })), module.exports = t;