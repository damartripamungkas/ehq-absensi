const e = require("path"), { worker: s } = require("cluster"), t = require("express").Router(), r = require("../utils/logger/log"), { time_zone: a, time_absen: o } = require(e.join(process.cwd(), "config", "config.json")), { isValidAccessKey: n } = require("../database/db"); t.post("/", (async (e, t) => { try { const { type: s, key: r } = e.body, i = await n(s, r); if ("main" == s) { const e = !0 === i ? { status: !0, data: { time_zone: a, time_absen: o } } : { status: !1 }; t.status(200).json(e) } else if ("admin" == s) { const e = !0 === i ? { status: !0, data: { time_zone: a, time_absen: o } } : { status: !1 }; t.status(200).json(e) } else t.status(200).json({ message: "wrong value input" }) } catch (e) { r.fatal(`[worker: ${s.id}] unhandleError ./src/backend/routes/login.js / : ${e}`), t.status(500).json({ message: "internal server error" }) } })), module.exports = t;